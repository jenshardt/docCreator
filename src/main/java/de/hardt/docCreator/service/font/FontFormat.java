package de.hardt.docCreator.service.font;

/**
 * Technical font program format, used to decide how a font is parsed and
 * embedded by {@link PdfFontService}.
 */
public enum FontFormat {
    TRUE_TYPE,
    TYPE1
}
